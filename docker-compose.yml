version: "3.9"

services:
  spent:
    image: spent:local
    build: .
    container_name: spent
    ports:
      - "8067:8067"
    volumes:
      - spent-data:/app/data   # SQLite persistence ONLY
    restart: unless-stopped

volumes:
  spent-data:
